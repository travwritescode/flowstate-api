[project]
name = "flowstate-api"
version = "0.1.0"
description = "Flowstate task API (FastAPI + SQLite)"
readme = "README.md"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
