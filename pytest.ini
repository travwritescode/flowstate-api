[pytest]
testpaths = tests
addopts = --cov --cov-fail-under=95

[coverage:run]
source =
    app/models
    app/utils/auth
    app/schemas
    app/config
    app/database
omit =
    app/dependencies.py

[coverage:report]
fail_under = 95
show_missing = true
